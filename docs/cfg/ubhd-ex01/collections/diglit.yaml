%YAML 1.2
# -*- coding: UTF-8, tab-width: 4 -*-
---

^:  # <-- Collection name. `^` means to use the basename of the filename.

    targetUrlMetadata:
        prefixes:
          - 'https://digi.ub.uni-heidelberg.de/diglit/'
        vSubDirs:
          - 'projectName'
          - 'pageNumStr'


...
