%YAML 1.2
# -*- coding: UTF-8, tab-width: 4 -*-
---

^:  # <-- Chain name. `^` means to use the basename of the filename.

  - if: always
    tendency:
        '*': deny
        discover: allow
        read: allow

  - if:
        memberOfAclGroup: '📚_authors'
        # Criterion `memberOfAclGroup` understands some emoji as placeholders.
        # 📚 is for serviceId.
    tendency:
        stamp_own_add_dc_dateSubmitted:  allow
        create:         allow
        reply:          allow

  - if:
        memberOfAclGroup: '📚_moderators'
    tendency:
        stamp_any_add_dc_dateAccepted:  allow
        stamp_any_add_as_deleted:       allow

  - if:
        memberOfAclGroup: '📚_publishers'
    tendency:
        stamp_any_add__ubhd_doiAssign:      allow




...
