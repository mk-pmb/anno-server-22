%YAML 1.2
# -*- coding: UTF-8, tab-width: 4 -*-
---

^:  # <-- Chain name. `^` means to use the basename of the filename.

  - if:
        memberOfAclGroup: '📚_authors'
    tendency:
        revise_own:             allow
        revise_own_creator:     allow
        revise_own_body:        allow
        revise_own_target_add:  allow
        revise_own_target_del:  allow

  - if:
        memberOfAclGroup: '📚_moderators'
    tendency:
        revise_any:             allow
        # revise_any_creator:     allow
        revise_any_body:        allow
        revise_any_target_add:  allow
        revise_any_target_del:  allow



...
