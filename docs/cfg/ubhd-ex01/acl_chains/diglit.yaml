%YAML 1.2
# -*- coding: UTF-8, tab-width: 4 -*-
---

^:  # <-- Chain name. `^` means to use the basename of the filename.

  - if: always
    tendency:
        '*': deny
        discover: allow
        read: allow

  - if: isLoggedIn
    tendency:
        create: allow




...
