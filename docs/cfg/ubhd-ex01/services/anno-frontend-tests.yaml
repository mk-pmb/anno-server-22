%YAML 1.2
# -*- coding: UTF-8, tab-width: 4 -*-
---

^:  # <-- Service name. `^` means to use the basename of the filename.

    targetUrlMetadata:
        prefixes:
          - 'http://anno.test/'
        vSubDirs:
          - 'projectName'
          - 'pageNumStr'


...
